# Sprint 0.1 — RBAC backlog (deferred within scope)

These staff-role templates are named in the master spec but cannot be seeded with a meaningful
permission set yet, because their permissions belong to features scheduled for later sprints. They
are recorded here (not implemented as empty roles) and will be added to StaffRoleTemplatesSeeder
once their permissions exist:

- media_manager  — awaits the Media Library (DAM) permissions (later sprint).
- moderator      — awaits the UGC moderation permissions (Reviews / Discussions / Q&A sprint).