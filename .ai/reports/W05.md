# W05 — Report

- status: in_progress (planning / re-scoping after rollback)
- title: Commerce / Entitlements

## Implemented changes
None merged yet. A first backend implementation was staged 2026-07-29 and rolled back;
artifacts retained at `../../_w05_removed/` (`EntitlementPort.php`, `w05_backend.tgz`,
`w05src.tgz`, `w05_backend.b64`). Nothing from W05 is in `main`.

## Verified fixes
n/a

## Tests added
n/a (pending re-implementation)

## Gate results
n/a (nothing merged)

## Remaining issues / open decisions
- Decide: re-apply `_w05_removed/` cleanly vs. rebuild from `docs/redesign/100_EXECUTION_BACKLOG.md` (Sprint 5 / B1).
- Confirm entitlement sources: paid `OrderCourseGrant` UNION active subscriptions.
- Ensure Learning consumes `EntitlementPort` only (scalar in/out); keep Deptrac clean.
- Gate new subsystem behind a capability/flag, default-off (ADR-06).

> Rewrite this report to the completed-wave template once W05 lands and gates are green.
