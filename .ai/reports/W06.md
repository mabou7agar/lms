# W06 Report — Launch-Blocker Hardening

## Scope (implemented)
Eliminate launch blockers across authz, routing, data integrity, security, prod readiness,
accessibility/RTL, performance, and tests. All fixes kept the 9 gates green.

## Verified fixes
Backend/security & integrity:
- Quiz-attempt IDOR/paywall bypass → enrollment gate on course-scoped assessments.
- Dunning gateway charge moved OUTSIDE the DB transaction + per-attempt idempotency key.
- Scheduled commerce workers `withoutOverlapping()->onOneServer()`.
- Coupon per-user/first-order rules re-asserted under the coupon row lock at checkout + DB `UNIQUE(order_id)` on coupon_redemptions.
- Trusted proxies fail closed by default (was `'*'`).
- Public coupon validation rate-limited (`commerce-coupon`).
- Fake media webhook registered only in local/testing (removed from production).
- Queued jobs got `$timeout` + `failed()` dead-letter handlers (session reminder, notification fan-out).
- Notification `per_page` clamped.
- Pagination added to unbounded list endpoints: my-learning, my-certificates, consulting, instructor courses (also bounds the per-course curriculum N+1), SEO admin list.
- Gradebook CSV export streamed in bounded roster chunks (byte-identical output).
- Author-HTML defense-in-depth: question presenter re-sanitizes with DOMPurify.

Frontend:
- Middleware protected-prefix list corrected (real routes; added profile/notifications/billing/subscriptions/admin/cart).
- Post-login lands on dashboard; footer links repointed to public pages.
- Commerce workspace rendered in the shared AppShell (sidebar + mobile drawer); public header got a mobile hamburger.
- `/teach/apply` reachable by any authenticated learner.
- Orphan gradebook page linked from the course actions menu.
- RTL physical→logical CSS properties across grading/submission components; header/topbar/mobile-nav aria-labels routed through the EN/AR dictionary.

## Tests
- Backend 800 passed (+4: payment idempotency, quiz enrollment gate). Frontend 484.

## Gate results (final)
- migrate:fresh --seed: PASS · PHPUnit: PASS (800) · PHPStan: PASS (0) · Deptrac: PASS (0) · Pint: PASS
- Typecheck: PASS · Lint: PASS (0 errors, 8 warnings) · Vitest: PASS (484) · Build: PASS

## Sync
- 39 files written to the device repo and verified byte-identical (SHA-256).

## Known limitations at W06 close
- A few low-value a11y items deferred (aria-label i18n on shared UI primitives; events tablist roving tabindex — operable).
- No git commit.
