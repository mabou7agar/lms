# W04 — Report

- status: complete (merged to `main`)
- milestone_commit: e494c6d — "W04 complete: Media, Learning Runtime, Assignments, Gradebook, Frontend integration"
- verified: true (present in git history; tracked tree clean at HEAD ed960b1)

## Implemented changes
- Media handling (Media Platform owns bytes; contexts hold references — ADR-08 direction).
- Learning runtime (lesson experience / quiz player wired to lessons).
- Assignments + Gradebook.
- Frontend integration for the above in `apps/web`.
Supporting commits in the same arc: 350b5ae (Course Builder), 335e1a8 (assessment domain + grading
engine), 5759e96 (learner quiz player), 8eafe57/3561c30 (publish-readiness engine + lifecycle),
b583fbd (per-course assessment pass-rate reporting port), 1552dbb (authorize analytics endpoints).

## Verified fixes
- 1552dbb — authorize the metric-driven analytics endpoints.

## Tests added
Per-story taxonomy (unit + integration + architecture; E2E where user-facing). Exact new-test
inventory not re-extracted in this batch — derive from the W04 diff if a precise count is needed.

## Gate results
Not captured into this layer for W04 specifically. Latest recorded full-green signal is CI run #13
(commit 89e57e7, "all seven mandatory jobs green — final GO"), which predates W04's feature commits;
a post-W04 full run should be captured into `verification/gate_history.json`.

## Remaining issues
See `context/project_state.json > known_issues` and `corelms/KNOWN_LIMITATIONS.md`.
